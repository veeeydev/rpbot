const ls = require('./lib');
const db = new ls('database');

db.once('ready', async () => {
	console.log('Session Ready!');
	
	const col = await db.collection({
    name:"collection"
  })

  col.once("ready", async () => {
    console.log(await col.findOne({data:1}))
  })
})