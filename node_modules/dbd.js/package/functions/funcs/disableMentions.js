module.exports = (d) => {
  
  let code = d.command.code
  
  for (const u of d.message.mentions.users.array()) {
    code = code.split(`<@${u.id}>`).join(u.username).split(`<@!${u.id}>`).join(u.username)
  }
  
  return {
    code: code.replaceLast(`$disableMentions`, "") 
  } 
} 