module.exports = async d => {
    const code = d.command.code

    const r = code.split("$toLowercase[").length - 1

    const inside = code.split("$toLowercase[")[r].split("]")[0]

    return {
        code: code.replaceLast(`$toLowercase[${inside}]`, inside.toLowerCase())
    }
}