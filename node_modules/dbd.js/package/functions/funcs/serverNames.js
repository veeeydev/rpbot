module.exports = async d => {
    const code = d.command.code 
    
    return {
        code: code.replaceLast(`$serverNames`, d.client.guilds.cache.map(g => g.name.deleteBrackets()).join(","))
    }
}