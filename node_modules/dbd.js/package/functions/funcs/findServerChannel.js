module.exports = d => {
  //gay const = dm.user.client.message(stfu);
  const code = d.command.code
  
  const r = code.split("$findServerChannel[").length - 1
  
  const option = code.split("$findServerChannel[")[r].split("]")[0]
  
  const manager = d.message.guild.channels.cache
  
  const c = manager.get(option) || manager.find(c => c.name.toLowerCase() === option.toLowerCase()) || d.message.mentions.channels.first() || d.message.channel
  
  return {
    code: code.replaceLast(`$findServerChannel[${option}]`, c.id)
  } 
} 