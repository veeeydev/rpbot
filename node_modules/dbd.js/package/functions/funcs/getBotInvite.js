module.exports = async d => {
    return {
        code: d.command.code.replaceLast(`$getBotInvite`, `https://discord.com/oauth2/authorize?client_id=${d.client.user.id}&scope=bot+applications.commands&permissions=8`)
    }
}