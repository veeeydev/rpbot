module.exports = d => {
 
  const code = d.command.code
  
  const r = code.split("$isNumber").length - 1
  
  const inside = code.split("$isNumber[")[r].split("]")[0]
  
  const n = Number(inside) 
  
  return {
    code: code.replaceLast(`$isNumber[${inside}]`, inside === "" ? false : isNaN(inside) ? false : true)  
  } 
} 