module.exports = async d => {
    const code = d.command.code

    const inside = code.split("$thumbnail[")[1].split("]")[0]
 
    d.embed.setThumbnail(inside.addBrackets() || undefined) 
  
    return { 
      embed: d.embed,
        code: code.replaceLast(`$thumbnail[${inside}]`, "")
    }
}