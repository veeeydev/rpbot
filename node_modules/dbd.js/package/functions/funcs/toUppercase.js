module.exports = async d => {
    const code = d.command.code

    const r = code.split("$toUppercase[").length - 1

    const inside = code.split("$toUppercase[")[r].split("]")[0]

    return {
        code: code.replaceLast(`$toUppercase[${inside}]`, inside.toUpperCase())
    }
}