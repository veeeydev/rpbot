module.exports = async d => {
    const code =d.command.code 
    
    if (d.message.guild.members.cache.size !== d.message.guild.memberCount)await d.message.guild.members.fetch()
    
    return {
        code: code.replaceLast(`$cacheMembers`, "")
    }
} 