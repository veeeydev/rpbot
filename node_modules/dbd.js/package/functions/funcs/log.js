module.exports = async d => {
    const code = d.command.code

    const r = code.split("$log[").length - 1

    const inside = code.split("$log[")[r].split("]")[0]

    console.log(inside.addBrackets())
    
    return {
        code: code.replaceLast(`$log[${inside}]`, "")
    }
}